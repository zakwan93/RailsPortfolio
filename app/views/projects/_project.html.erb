<div class="card" data-id="<%= project.id %>">
    <%= image_tag project.thumb_image.url unless project.thumb_image.nil? %>
    <p class="card-text">
        <span><%= link_to project.title, project_show_path(project) %></span> <%= project.subtitle %>
    </p>
</div>